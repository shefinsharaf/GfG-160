class Solution {
  public:
    void nextPermutation(vector<int>& arr) {
        //in c++ there is a built in function for finding the next permutation directly
    next_permutation(arr.begin(),arr.end());
    }
};